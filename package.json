{
  "name": "makeup-workspace",
  "private": true,
  "scripts": {
    "dev": "cd app && npm run dev",
    "build": "cd app && npm run build",
    "preview": "cd app && npm run preview"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^6.0.2"
  }
}
